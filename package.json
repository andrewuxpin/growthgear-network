{
  "name": "growthgear-network",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules",
    "typecheck": "turbo typecheck",
    "sync:articles": "npx tsx scripts/sync-articles.ts",
    "db:migrate": "echo 'Run migrations with: wrangler d1 execute growthgear-db --file=./database/migrations/XXX.sql'"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
